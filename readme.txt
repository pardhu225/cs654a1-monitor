* Requires Node.JS
* Run
    npm install
  in the root folder of the project to install all the dependencies
* Run 
    npm run start
  to start the server.
* Add the new nodes IP and validation details to nodes.json file and restart the server